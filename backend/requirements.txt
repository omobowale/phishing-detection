fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
email-validator>=2.1
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7
bcrypt==4.0.1  # passlib 1.7.4 breaks on bcrypt>=4.1 (removed `__about__`, stricter 72-byte check)
python-multipart>=0.0.9
scikit-learn==1.9.0  # pinned exactly, not >=: affects exact ml/README.md numbers, not just API compat
xgboost==3.4.1       # same reason
transformers>=4.40
torch>=2.2
accelerate>=1.1.0  # required by transformers.Trainer (ml/training/train_email_bert.py) --
                    # this transformers/torch version needs >=1.1.0 specifically, not just
                    # "some accelerate"; a bare >=0.26 install fails at Trainer construction
nltk>=3.8
pandas==3.0.5        # same reason -- also the version this project's pandas-3.0 groupby-apply
                      # behavior-change fix (ml/training/build_features.py) was written against
numpy==2.5.2          # same reason
python-dotenv>=1.0
pytest>=8.0
httpx>=0.27
kaggle>=1.6
joblib>=1.4
pyarrow>=16.0  # parquet read/write for pandas
tldextract==5.3.2  # pinned exactly (not >=): bundles a Public Suffix List snapshot that changes
                    # between releases, which would silently change the domain-grouped split's
                    # composition run to run on a different install -- see build_features.py
